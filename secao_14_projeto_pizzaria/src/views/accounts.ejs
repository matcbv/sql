<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/global.css">
    <link rel="stylesheet" href="/css/tailwind/output.css">
    <title>Demonstrando SQL</title>
</head>
<body class="max-w-100 h-screen bg-purple-400">
    <header class="bg-purple-500 px-10">
        <div>
            <ul class="flex gap-x-10 list-none">
                <li><a href="/">Home</a></li>
                <li><a href="/accounts">Contas</a></li>
            </ul>
        </div>
    </header>
    <main class="px-8">
        <h1 class="my-8">Contas</h1>
        <% result.forEach(element => { %>
            <div class="flex items-center mb-8">
                <div>
                    <p><%= element.nome %></p>
                    <p><%= element.sobrenome %></p>
                    <p><%= element.cpf %></p>
                </div>
                <button type="button" class="del-account ml-4 h-8 px-4 rounded-full">Deletar conta</button>
            </div>
        <% }) %>
    </main>
    <script src="/js/index.js"></script>
</body>
</html>